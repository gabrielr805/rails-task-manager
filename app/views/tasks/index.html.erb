<h1>Test of index view </h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
  <%= link_to task.title, task_path(task) %>
  <%= task.title %>
  <%= task.details %>
  <%= task.completed %>
  </li>
  <% end %>
</ul>


